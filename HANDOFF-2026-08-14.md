# Status — 14 Aug 2026

40 commits on `session/money-spine-hardening-jun1`, **pushed**.
**1102 unit tests green · e2e 80 passed / 70 skipped / 0 failed · typecheck clean · lint 0 errors.**

---

## ✅ Done today

### Email — this is the big one
- [x] Gmail OAuth consent flow (`/api/integrations/google-gmail/connect` + `/callback`)
- [x] Callback **refuses** if `gmail.send` wasn't granted, instead of storing a token that 403s later
- [x] Gmail status route — answers *"can we send"*, not *"are we connected"*
- [x] Per-tenant Resend key in `tenant_secrets` (encrypted) + `email_from_address`
- [x] Provider switch API — **refuses to select a transport that cannot send**
- [x] Settings → Integrations → **Email sending** card
- [x] Gmail switched ON for this tenant
- [x] **First real email sent** — Gmail id `19ffc9c8fff7efe8`
- [x] Central `email_log`, written from *inside* `sendEmail()` so no caller can skip it
- [x] Test-send endpoint that can only ever email the signed-in user

### Password vault
- [x] `/api/vault` (list + create) — returns **no ciphertext**, ever
- [x] `/api/vault/[id]/reveal` — logs **before** revealing; failed log = no reveal
- [x] `/vault` page + sidebar entry (Admin & Control), owner/manager only
- [x] `fingerprintPassword()` — HMAC under the master key, not a bare hash

### Reports
- [x] MRR waterfall — Expansion/Contraction render `—`, **never ₹0** (not tracked ≠ nothing happened)
- [x] `mrr_movements` ledger migration (0236) — trigger-based, not app-code
- [x] CSV hardened: formula-injection guard + UTF-8 BOM (4 accounting pages used it, untested)
- [x] Print-to-PDF via print CSS, **no new dependency**

### Nav / UI
- [x] Marketing group in sidebar (page existed for a session, unreachable)
- [x] nav↔filesystem tests — found a duplicate nav id + a missing breadcrumb
- [x] Leads page: empty band under TopBar fixed (two causes, both needed)
- [x] Draft protection on Add Lead + quote builder

### Migrations applied
- [x] `0233` access register · `0234` vault · `0235` email provider · `0237` resend key · `0238` email_log

---

## 🔴 Was broken — fixed, but YOU still need to confirm

- [x] **Tab switching changed the URL without navigating.** Cause was raw
      `history.replaceState`, which rewrites the address bar without telling the App
      Router. Now `router.replace` (`1d7ccdc`).
- [x] **It still looked broken after the fix** — because dev compiles a route on first
      visit and nothing on screen said so. The strip now shows a 1px bar while a switch
      is resolving (`5659c2e`).
- [x] **Found out WHY it may never have appeared** (14 Aug). `isNavigating` was read into
      the tabs context object but was **missing from that `useMemo`'s dependency list**, so
      every consumer got whatever the flag had been the last time `state.tabs` changed
      identity. The bar was driven by a stale value and could simply never show. Fixed in
      `a12de41` — and it had been sitting in `npm run lint` as a warning the whole time
      (`workspace-tabs-provider.tsx:361`), which is its own lesson about warnings.
- [x] **The bar works — measured with real tabs open** (14 Aug, dev, 1280×800). Opened
      several workspace tabs and instrumented the strip, timing how long the bar element
      is actually in the DOM across a switch:

      | switch to | bar visible for |
      |---|---|
      | `/customers` (not yet compiled) | **2,839 ms** |
      | `/marketing/reports` (not yet compiled) | **8,350 ms** |
      | back to `/leads` (already compiled) | **95 ms** |

      It appears 5–11 ms after the click and disappears the moment the page arrives —
      exactly the behaviour this was built for, and exactly what the old note predicted
      ("click back — that one should be instant").

      ⚠️ Honest limit: I have **not** got a screenshot with the bar visibly in frame. The
      bar is 2px (`h-0.5`) and the capture round-trip consistently lands after the route
      resolves. The timings above are DOM-presence measurements, which are stronger
      evidence than a screenshot — but if you want to see it with your eyes, open a tab
      to a page you have not visited this session and watch the bottom edge of the strip.
- [x] **Ctrl+click is NOT broken — it was the harness.** A synthetic
      `MouseEvent('click', {ctrlKey:true})` on an internal link creates the background tab
      correctly, every time. What fails is the automation driver: its ctrl modifier never
      reaches the page, so the click arrives unmodified. The interception code
      (`workspace-tabs-provider.tsx:266-301`) is fine and does not check `isTrusted`.
      Nothing to fix in the app.

---

## 🔵 Found and fixed 14 Aug while chasing a "deploy is broken" report

The deployed `/leads` showed **"Something went wrong"**. Two separate causes, neither
of them the deploy:

1. **Browser cache poisoning (not an app bug at all).** Supabase REST sends no
   `Cache-Control` and no `Vary: Origin`, and echoes the request Origin into
   `Access-Control-Allow-Origin`. So the cache key is just the URL, and a response
   cached while on `http://localhost:3000` gets replayed to the Cloud Run origin still
   carrying `Access-Control-Allow-Origin: http://localhost:3000` — every client-side
   query fails CORS. Proven live: `fetch(url,{cache:'default'})` blocked,
   `{cache:'reload'}` fine. Fixed by pinning `cache: "no-store"` on the browser
   Supabase client (`49b809e`), the twin of the rule the admin client already followed.
   **Only hits a machine that visits both origins** — a customer never can. It costs
   debugging sessions, not customers.
   *If it recurs before the deploy lands: Ctrl+Shift+R.*

2. **An unbounded render loop of ours, which is why the page DIED instead of degrading.**
   `useDraftGuard` calls `setDraft` from an effect that depends on `setDraft`; the
   reducer always returned a new `tabs` array even for a no-op write; the provider
   rebuilt every callback whenever `tabs` changed identity. New array → new `setDraft`
   → effect fires → new array. No exit. React threw #185 at 50 nested updates, and that
   secondary error escaped the `(app)` error boundary **while it was mounting**, so the
   operator lost the whole shell and got bare `global-error.tsx`. Fixed in `a12de41`
   (reducer bails out on no-op · api callbacks are stable · `isNavigating` dep restored),
   with 6 reference-identity tests. Reproduced and verified before/after in the browser.

   ⚠️ **Worth internalising:** the app error boundary is designed to keep the operator
   inside the app with a way out (§24). It never got the chance. Any error thrown while
   `error.tsx` mounts skips it entirely and lands on the bare full-page fallback — so a
   "Something went wrong" with **no sidebar** means *two* things broke, not one.

## 🟡 Pending — in order

1. [x] **Data reset — no longer needed, and this was checked before deploying.**
       The dashboard (same Supabase project as production) reads **0 customers, 0
       subscriptions, 0 renewals in 30 days**, with the "add your first customer"
       onboarding checklist showing. The 54 dummy subscriptions that made a deploy
       dangerous are gone, so the renewal cron has nothing to email and the "sent email
       cannot be recalled" risk did not apply.
       *Scope of that check: read through the app UI, which RLS scopes to this tenant —
       so it means "0 for this tenant", not "0 rows in the table". That is the tenant the
       blocker was about.*
       The batches below are still there if a future reset is wanted:
       - Back up FIRST: `node scripts/backup-tenant-data.mjs`
       - Batches 1, 2, 5 only. **Batches 3 and 4 are REAL** (bank/accounting, payroll) —
         deliberately not extracted.
       - Blocks the deploy: once deployed, the renewal cron emails whatever is in those
         54 dummy subscriptions, and **sent email cannot be recalled**.
2. [x] **Push** — done. `a20dc01..5659c2e`, then `e0bd74b..69bdd68` on the `anutech` remote.
3. [x] **DEPLOYED 14 Aug** — revision `resellersos-00285-l2c`, serving 100% of traffic.
       Production is no longer running old code.

       Verified: the deployed app serves `/leads` cleanly and redirects to login when
       signed out, with **zero CORS and zero React errors** in the console — the exact
       page that was dying in `global-error` before today's two fixes.

       ⚠️ NOT verified: anything behind the login. I cannot sign in (entering a password
       is not something I do), so the authenticated pages on production are yours to
       click through. Worth a pass over Settings → Integrations → Email sending first,
       since that is the newest surface.
4. [ ] Register the Cloud Run Gmail redirect URI (localhost one is done):
       `https://resellersos-1005662057478.asia-south1.run.app/api/integrations/google-gmail/callback`

---

## 🟢 After the migrations — three code cleanups

These are not tidying. Each is a place where code can now silently disagree with the DB.

- [ ] Regenerate `src/lib/supabase/types.ts` (needs a Supabase access token — I don't have one)
- [ ] Delete `src/lib/vault/db.ts` — hand-written row shapes, only needed while 0234 was unapplied
- [ ] Remove the `secretsTable` cast in `src/app/api/integrations/seal/route.ts`

---

## 🐞 Found in production data — FIXED 14 Aug (and the diagnosis above it was wrong)

- [x] **email_log mislabelled the provider on a failed Gmail send.**

      ⚠️ **The original entry here blamed the wrong line and told the wrong story.**
      It said `send.ts:135` (the `decision.blocked` path) hardcoded `provider: "resend"`,
      and that "the resolver fell back to Resend and the log recorded the fallback".
      Both wrong, and worth keeping visible because the wrong story was the plausible one.

      **What actually happened.** Line 135 could never have produced that row: it logs
      `decision.blocked` verbatim, and every possible blocked string is composed in
      `provider.ts` and ends in *"…cannot be sent at all"* / *"No email provider is
      configured…"*. None can start with `Gmail reauth_required`. Only one line in the
      codebase emits that shape — the Gmail-failure return, `` `Gmail ${r.failure}: ${r.detail}` ``
      — and **that return omitted `provider` entirely**, so the `?? "resend"` default at the
      `recordEmail` call filled in a confident wrong answer.

      **So there was no fallback.** Gmail was chosen, Gmail was attempted, Gmail failed, and
      `sendEmailInner` returned right there. Resend never saw the message. The row was not
      mislabelling a fallback — it was mislabelling a send that never happened, which also
      means **that email did not go out at all**.

      **Fixed:**
      - Gmail-failure return now says `provider: "gmail"`.
      - `EmailSendResult.provider` is now **required**, and the `?? "resend"` default is gone.
        A return statement that forgets it no longer compiles. This is the actual fix — the
        one-line version would have left the next return point free to make the same mistake.
      - The blocked path now records **the provider the tenant CHOSE** (new
        `ProviderDecision.requested`), not the resolver's fallback. That was the design call
        the old entry asked for: when nothing sent, "which one carried it" has no answer, and
        the tenant's choice is both the only true thing left and the thing an operator
        searches by.
      - Resend + stub paths now state their provider explicitly instead of relying on the default.

      `src/lib/email/send.test.ts` — 7 tests, including the exact production row inverted.

- [ ] **Still open, needs a migration: a SILENT fallback is invisible in SQL.**
      When a tenant is on Gmail, Gmail is unusable, and Resend *is* configured, the message
      really does go out via Resend — so `provider=resend, status=sent` is truthful. But it
      means a tenant whose Gmail broke weeks ago looks perfectly healthy in `email_log`, and
      the only trace is a `console.warn` nobody reads. `where provider='gmail'` finds nothing
      because nothing was sent as Gmail. Wants a `requested_provider` (or `fell_back`) column
      on `email_log`; `decision.requested` already carries the value, so it is a migration
      plus one field, not a redesign.

---

## ⚪ Later

- [ ] Resend key + domain verification — **Gmail reports no bounces at all**, so a dead
      customer address fails silently and `email_log` still says `sent`
- [ ] `email_from_address` is null
- [ ] Resend delivery webhooks — `status='sent'` means *the provider accepted it*, not delivered
- [ ] Migrations `0231` (performance points), `0232` (marketing attribution), `0236` (mrr_movements)
      — written, unapplied, nothing waiting on them
- [ ] Vault has no delete/edit yet — only add + reveal
- [ ] Access Register (`0233` applied) has no UI page at all
- [ ] Export buttons only on `/accounting/saas-metrics`. Still missing on `/reports/profit`,
      `/accounting/aging`, `/accounting/tds-receivable`, `/performance`
- [x] `npm run test:e2e` — **run**: 80 passed, 70 skipped, 0 failed. The 70 skips are auth-gated
      specs, so this is not full coverage — but it is no longer unrun.

---

## Three things I got wrong today, so they don't get re-learned

1. **Removed `include_granted_scopes` from the Gmail flow** reasoning the scopes column should
   record only what was asked. Backwards — both flows write the same `user_google_tokens` row,
   so it would have stored a send-only token and **broken contacts sync**. Caught before shipping.
2. **Set the leads sticky header to `top-0`**, which reintroduced the exact TopBar collision
   reported earlier. Both causes had to be fixed together.
3. **Measured layout at an unknown scroll position** and drew a confident wrong conclusion —
   twice. The fix: `scrollTo(0,0)`, measure, scroll, measure again, and check `h1` to confirm
   you're on the page you think you are.
